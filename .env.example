# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Weather API Configuration
# Using Open-Meteo (https://open-meteo.com/) - FREE and NO API KEY REQUIRED!
# Weather data is fetched automatically for events with locations.
# No configuration needed!

# Google Calendar Configuration
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_REDIRECT_URI=http://localhost:5001/api/google-calendar/callback
CLIENT_URL=http://localhost:3000

# Server Configuration (optional)
# Note: Don't use PORT here as it affects the React client too
# The server uses PORT from process.env with default 5001 in server.js
# PORT=5001